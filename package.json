{
  "name": "spazz-fixed",
  "private": true,
  "scripts": {
    "build": "echo \"Static deployment: no build step required\"",
    "dev": "python -m http.server 8000"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}